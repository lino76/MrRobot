The second program contains an invalid comment (comments on its own line must not be preceded by a space). Therefore, the second program should fail and be rolled back. Thus, the password given for bob in the third program should be rejected and the server should return DENIED. However, the target returns RETURNING for the third program. 
