Here we have an availability security violation. When attempting to delegate access with insufficient privilages a FAILED response should be produced (oracle behavior). Instead we get a DENIED status
